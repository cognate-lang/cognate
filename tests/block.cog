Define Baz as
(
  Let Z;
  (Do Z);
);


Define Bar as
(
  Let Y;
  Baz (Do Y);
);

Define Foo as
(
  Let X;
  ~~Set X to X;
  Bar (Print X);
);


Foo 'PASS: Implicit block copying';
Foo 'FAIL: Implicit block copying';
Drop, Do;


Let Y be ();

Define Foo1 as
(
  Let X;
  Set Y to (Print X);
);


Foo1 'PASS: Implicit block copying with mutation', Let Z be Y;
Foo1 'FAIL: Implicit block copying with mutation', Let W be Y;
Do Z;
Drop W;

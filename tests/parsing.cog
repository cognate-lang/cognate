~ Let A be 4; ~ ~~ Let A be 4;
Let A be 5; ~~ Let A be 4; ~ Let A be 4; ~
~ Let A be 4; ~ ~~ Let A be 4;

Print If == 5 A
 "PASS: Comments"
else
 "FAIL: Comments";

~~ The syntax highlighter can"t handle this next bit.

Let X be Box 0;

Def Inc as ( Set X to + 1 Unbox X );

" \" Inc; \" " " " Inc; " "
Inc; "" "Inc;"
" Inc; \"  Inc ";
Clear;

Print If == 2 Unbox X
  "PASS: Strings"
else
  "FAIL: Strings";

Print If == 1.5 / 2 3
  "PASS: Floating point numbers"
else
  "FAIL: Floating point numbers";

Let B be ( Print "hello" );
Let B2 be ( Print "hello" );

Let T be Table (
	\foo is "bar"
	"bar" is \foo;
	12 is 13;
	B is \hello;
	B2 is \goodbye;
);

Print If == "bar" . \foo T
	"PASS: Table lookup 1"
else
	"FAIL: Table lookup 1";

Print If == \foo . "bar" T
	"PASS: Table lookup 2"
else
	"FAIL: Table lookup 2";

Print If == 13 . 12 T
	"PASS: Table lookup 3"
else
	"FAIL: Table lookup 3";

Print If == \hello . B T
	"PASS: Table lookup 4"
else
	"FAIL: Table lookup 4";

Print If == \goodbye . B2 T
	"PASS: Table lookup 5"
else
	"FAIL: Table lookup 5";

Let T2 be Insert \foo is "baz" into T;

Print If == "baz" . \foo T2
	"PASS: Table insertion"
else
	"FAIL: Table insertion";

Print If == "{ foo:\"bar\" \"foo\":bar }" Show Table ( \foo "bar" and "foo" \bar )
	"PASS: Printing a table"
else
	"FAIL: Printing a table";

Print If And Has \foo T Not Has \bar T
	"PASS: Has"
else
	"FAIL: Has";

Let X be Box Table ();

For each in Range 1 to 1000 ( Let I ; Set X to Insert I is I into Unbox X );
For each in Range 1 to 1000 ( Let I ; Drop Has I Unbox X );

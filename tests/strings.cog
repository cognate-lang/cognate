If (Characters in 'Hello ☺', == List ('H','e','l','l','o',' ','☺')) (
  Print 'PASS: Extracting characters from strings'
);
Else (
  Print 'FAIL: Extracting characters from strings'
);

If (Join with ' ☺ ' List ('Hello','world'), == 'Hello ☺ world') (
  Print 'PASS: Converting list of strings to combined string'
);
Else (
  Print 'FAIL: Converting list of characters to combined string'
);

If (Ordinal of '☺', == 9786) (
  Print 'PASS: Converting string to UTF8 value with Ordinal'
);
Else (
  Print 'PASS: Converting string to UTF8 value with Ordinal'
);

If (Character with value 9786, == '☺') (
  Print 'PASS: Converting UTF8 value to string'
);
Else (
  Print 'FAIL: Converting UTF8 value to string'
);

If (Number '123.4', == 123.4) (
  Print 'PASS: Casting strings to numbers'
);
Else (
  Print 'PASS: Casting strings to numbers'
);

If ('Hello λ world λ testing', Split on ' λ ', == List('Hello', 'world', 'testing')) (
  Print 'PASS: Splitting a string with a multi-character delimiter';
);
Else (
  Print 'FAIL: Splitting a string with a multi-character delimiter';
);

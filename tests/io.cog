~~ If print is broken, we probably know by now.
Read 'tests/io.txt';
If (== 'foo\nbar') (
  Print 'PASS: Reading multi-line file to string'
);
Else (
  Print 'FAIL: Reading multi-line file to string'
);

#!/usr/bin/env bash
# This script will automatically install CognaC's dependencies on macOS systems
brew install ghc clang-format cabal-install bdw-gc
cabal update
cabal install --lib split
ghc cognac.hs

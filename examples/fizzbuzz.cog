~~ See www.wikipedia.org/wiki/Fizz_buzz
Define FizzBuzz
( Let N be a number;
  Define Multiple (Let X; Modulo X N; == 0);
  Print If (Multiple of 15) then 'FizzBuzz'
        If (Multiple of 3)  then 'Fizz'
        If (Multiple of 5)  then 'Buzz'
                            else N
);

(FizzBuzz); For each in Range from 1 to 100 step 1;

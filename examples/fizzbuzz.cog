Def Fizzbuzz (
	Let N;
	Def Multiple as ( Let X ; Zero? Modulo X N );
	Print
		If Multiple of 15 then "fizzbuzz"
		If Multiple of 3  then "fizz"
		If Multiple of 5  then "buzz"
		else N;
);


For each in Range 1 to 100 ( Fizzbuzz );

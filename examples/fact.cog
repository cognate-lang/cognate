Import Builtin;

Define Factorial
( Let X;
  If (X, == 0) (
    1
  );
  Else (
    Subtract 1 from X;
    find the Factorial;
    Multiply by X;
  )
);

Put 'Enter an integer: ';
Input, ParseNumber, Twin;
Assert 'Input is an integer' Integer?;
 ~~ Syntax highlighter breaks here, but actual parser is fine
Put 'It\'s factorial is... ';
Print the Factorial;

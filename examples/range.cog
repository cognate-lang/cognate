Let (When P A) be (Do Do Do P A ());

Let (the Range from A to B with step S) be
~ Similar to python's xrange() function ~
(
  List from
  (
    ~ Nested helper function, invisible outside its scope ~
    ~ Will stack overflow on large ranges due to lack of TCO ~
    Let (Count from X) be
    (
      When (X, <= B)
      (
        return X; 
        Count from + X S;
      );
    );
    Count from A;
  );
);

Print the List (0,2,4,6,8,10,12,14);
Print the Range from 0 to 15 with step 2;

~~ This is a simple email validator.

Put 'Enter an email: ';
Let Email be Input;

Print If (Email; does Match-regex '^[[:graph:]]+@[[:graph:]]+\\.[[:graph:]]+$')
  'This is a valid email!'
else
  'This is an invalid email!';

~~ This is a simple email validator.

Put "Enter an email: ";
Let Email be Input;

Print If Match Regex "^[[:graph:]]+@[[:graph:]]+\\.[[:graph:]]+$" Email
	"This is a valid email!"
else
	"This is an invalid email!";

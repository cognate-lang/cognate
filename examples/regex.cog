~~ This is a simple email validator.

Put 'Enter an email: ';
Let Email be Input;

If [Email, does Match '^[[:graph:]]+@[[:graph:]]+\\.[[:graph:]]+$']
[
  Print 'This is a valid email!';
];
Else [
  Print 'This is an invalid email!';
];
